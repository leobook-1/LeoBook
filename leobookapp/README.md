# LeoBook App (v3.6) — High Density Liquid Glass Dashboard

**Chief Engineer**: Emenike Chinenye James
**Goal**: Ship extreme UI density with premium translucency and zero hardcoded overflows.

---

## The v3.0 Design Philosophy

The v3.0 rebuild follows the **"Telegram Aesthetic"** — maximizing information density without sacrificing visual elegance.

### 1. High Density UI
- **80% Size Reduction**: All elements (icons, text, cards) have been scaled down to professional "elite" proportions.
- **Micro-Radii**: Border radii reduced from 24dp → 14dp for a sharper, modern edge.
- **Glass-First**: 60% translucency `BackdropFilter` containers with 16 sigma blur.

### 2. Proportional Scaling (Constraints-Based Design)
We have eliminated hardcoded pixel values (e.g., `width: 300`). 
- **`Responsive.sp(context, value)`**: Scales based on viewport width relative to a 375dp reference.
- **`Responsive.dp(context, value)`**: Handles desktop-specific scaling (1440dp reference).
- **Parity**: Mobile, Web, and Desktop share the same responsive foundation.

---

## Core Features

- **Liquid Glass Containers**: Premium frosted effects using the `GlassContainer` widget.
- **4-Tab Match System**: `ALL` | `LIVE` | `FINISHED` | `SCHEDULED` tabs.
- **Real-Time Streaming**: Syncs with `fs_live_streamer.py` (v2.1) output for minute-by-minute updates.
- **Accuracy Tags**: Dynamic prediction success indicators fed by the backend review pipeline.
- **Constraints-Based Category Bar**: Horizontal scrollable categories that adapt to available width.

---

## Technical Stack

- **State Management**: Riverpod 2+
- **Architecture**: Clean Architecture (Data → Logic → Presentation)
- **Responsive System**: Custom `lib/core/constants/responsive_constants.dart`
- **Backend**: Supabase + Local CSV Handshake

---

## Development Workflow

### Commands
```powershell
flutter pub get
flutter run -d chrome  # For web preview
flutter build web --release --web-renderer canvaskit
```

### Constraints Rule
> **Rule**: Never use a fixed `double` for layout-critical spacing. Use `Responsive.sp()` or `Responsive.horizontalPadding()`.

---
*Generated by Antigravity AI · Matterialless LLC*
